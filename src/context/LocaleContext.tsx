"use client";
import { createContext, ReactNode, useContext, useEffect, useState } from "react";
import { formatMessage, isLocale, Locale, locales, messages } from "@/lib/i18n";
type Value={locale:Locale;setLocale:(locale:Locale)=>void;t:<K extends keyof typeof messages.en>(key:K)=>typeof messages.en[K];format:(template:string,values:Record<string,string|number>)=>string};
const Context=createContext<Value|null>(null); const KEY="idr-locale";
export function LocaleProvider({children}:{children:ReactNode}) { const [locale,setCurrent]=useState<Locale>("en"); useEffect(()=>{const saved=localStorage.getItem(KEY);if(isLocale(saved))setCurrent(saved)},[]); const setLocale=(next:Locale)=>{const safe=isLocale(next)?next:"en";localStorage.setItem(KEY,safe);setCurrent(safe)};useEffect(()=>{document.documentElement.lang=locale},[locale]); const t=<K extends keyof typeof messages.en>(key:K)=>messages[locale][key] as typeof messages.en[K]; return <Context.Provider value={{locale,setLocale,t,format:formatMessage}}>{children}</Context.Provider> }
export function useLocale(){const value=useContext(Context);if(!value)throw new Error("useLocale must be used within LocaleProvider");return value}
export function LanguageSwitcher(){const {locale,setLocale,t}=useLocale();return <label className="flex items-center gap-2 text-sm"><span className="sr-only">{t("language")}</span><select aria-label={t("language")} value={locale} onChange={(event)=>setLocale(isLocale(event.target.value)?event.target.value:"en")} className="rounded border border-slate-600 bg-slate-900 px-2 py-1"><option value="en">English</option><option value="th">ไทย</option></select></label>}
